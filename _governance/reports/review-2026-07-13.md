# Pure Review — 2026-07-13 — cockpit command-center redesign
Branch: feature/cockpit-command-center (c96baa5) vs base tag review/2026-07-13-dashboard-wse.
Diff: review-2026-07-13-diff.txt (1680 lines). Two fresh isolated reviewers (Task-tool
subagents), inputs limited to VISION.md + SPEC.md + the diff.

**Both verdicts: ALIGNED-WITH-FLAGS. No MISALIGNED, no A/B verdict disagreement.**
Owner decides on the flags at merge time per ritual §5.

---

## Reviewer A (vision advocate) — VERBATIM

# Pure-Review Findings — Reviewer A (Vision Advocate)
**Inputs read:** VISION.md, SPEC.md, review-2026-07-13-diff.txt (all 1681 lines). Nothing else.

**Diff scope at a glance:** Entirely within `cockpit/`. A cockpit visual/IA overhaul (Chrome nav, globals.css design system, next/font wiring), two new pages (`/money`, `/ops`), a rebuilt `/` "Today" triage page, two new libs (`money.ts`, `attention.ts`), and their tests. Every remaining hunk is a one-line `active="…"` tab-key retarget following the nav restructure.

---

## (a) SCOPE FIDELITY — 4/5

Most of the diff traces cleanly to blessed scope:

- **Money view + `/money` page + `money.ts`** → SPEC WS-E "Expanded dashboard … visual representations … Renders from registers" and Slice-1 item 3 "See — register views (subscriptions …)". It reads only the subscriptions register and, per its own header, *"invents nothing: every dollar figure shown is a figure already in the register."* Matches VISION power 3's "subscriptions and spend (with the cumulative-vs-ceiling picture explicit)" — the page renders `$${spend.spentUsd} of $${spend.ceilingUsd} agent ceiling`.
- **Attention queue / "Today" page + `attention.ts`** → WS-E "live fabric status" + VISION power 3 "richer than today." The lib header binds it to canon: *"every item traces to a calendar row with an open action, a pending decision file, a subscription the register itself flags, or an asset the register marks as a single copy."*
- **Per-project footprint block on Today** (`PROJECTS`, `buildProjectFootprint`) → WS-E "per-project footprint tracking" + VISION power 4. (Source lib is pre-existing, not in this diff.)
- **`/ops` hub + the twelve `active="/ops"|"/money"` retargets** → mechanical consequences of the nav restructure; each just re-keys an existing page to its new tab.

**Untraceable / sideways changes (flagged, not blocking):**
1. **Design-system overhaul** — `globals.css` swaps the entire palette (GitHub-blue → warm charcoal + brass) and `layout.tsx` loads three Google fonts (Barlow / Barlow Semi Condensed / IBM Plex Mono). No SPEC acceptance criterion or scope line calls for a visual re-skin; WS-E names "mockups/visual representations" of *data*, not a chrome restyle. **Sideways-but-harmless** — re-presents the same register data without adding or removing a capability.
2. **Top-nav IA change** — `Overview→Today`, adds `Money`/`Ops`, drops `Registers`/`Status`/`Reports` as top-level tabs (folded into the `/ops` hub). Defensible as WS-E reorganization, but the demotion is a navigational judgment not spelled out in SPEC. Nothing is lost (all reachable via `/ops`), so **sideways-but-harmless**.

## (b) NON-GOAL VIOLATIONS — 5/5

None. Checked against every VISION and SPEC non-goal:
- No second user / public face — still single-owner cockpit chrome.
- Not a credentials store/viewer — `MoneyItem` omits `payment_last4` entirely (never rendered); `notes` are passed through `redactForDisplay(...)` before display on both the money page and the attention queue.
- No automation pressing tokens or self-approving — the decision surface is unchanged; new pages are read-only renders.
- Review gate untouched; no speed-for-gate trade.
- No new top-level planning documents (see (d)).

## (c) CANON TAMPERING — 5/5

The diff touches **none** of VISION.md, SPEC.md, PORTFOLIO.md, the agent-instruction file (`CLAUDE.md`), or enforcement config. Because there is no SPEC edit, the intake-log requirement is not triggered. Clean.

## (d) RESIDUE CREATION — 5/5

No persistent directive docs added. Every new file is code (`.ts`) or tests (`.test.ts`) — no `.md` outside `_governance/`. Scanned all new-file comments: they are descriptive ("Money view — a classified picture of registers/subscriptions.csv", "The attention queue — the cockpit's triage board"), containing no directive language, roadmaps, task lists, or "flagship"/"we will"/"the plan is" phrasing.

---

## Advocate's read — does it move the blessed vision forward?

**Forward, not merely sideways** on the core: it materially advances VISION Layer-2 power 3 ("See — and richer than today … subscriptions and spend with the cumulative-vs-ceiling picture explicit") and power 4 (per-project footprint), which is exactly what WS-E blesses. The money classification and attention triage are net-new *capability* rendered honestly from the registers.

One sequencing observation for the record: SPEC lines 22–24 name WS-B (spend gate) and WS-A (orchestrator contract) as "the safety spine … built + verified before any autonomous run is enabled. GLM (WS-D) and the expanded dashboard (WS-E) follow." This diff advances WS-E. It does **not** enable an autonomous run, so it doesn't breach that gate — but it is WS-E work landing ahead of visible WS-A/WS-B completion. **Flag, not violation.**

---

## VERDICT: **ALIGNED-WITH-FLAGS**

No non-goal breach, no canon tampering, no residue. Flags are all sideways-but-harmless: (1) an unmandated visual/font re-skin, (2) a nav IA restructure not spelled out in SPEC, (3) WS-E advancing ahead of the stated WS-A/WS-B safety-spine sequence. Every data-bearing addition traces to WS-E, Slice-1 item 3, or VISION powers 3–4.

---

## Reviewer B (adversarial residue hunter) — VERBATIM

# Pure-Review — Reviewer B (adversarial residue hunter)

Inputs read: VISION.md, SPEC.md, and review-2026-07-13-diff.txt (all 1681 lines). Nothing else.

## Verdict: ALIGNED-WITH-FLAGS

The diff is a cockpit UI change: a new design system, a re-landed "Today" page with a synthesized attention queue, a new "Money" page classifying the subscriptions register, a new "Ops" hub, plus two libs (`attention.ts`, `money.ts`) and their tests. No canon touched, no non-goal advanced, no residue docs. The flags are all scope-fidelity: two sizable changes I cannot trace to a SPEC line.

---

## (a) SCOPE FIDELITY — 3/5

**Traceable (clean):**
- `/money` page + `money.ts` render/classify `registers/subscriptions.csv` → Slice 1 item 3 ("render the registers … subscriptions") and WS-E ("visual representations, reports"). Every figure is register-sourced: `"every dollar figure shown is a figure already in the register; unknown costs stay unknown"`.
- Spend-gate link on Money/Today → WS-B ledger. `active="/money"` rewiring of `/spend`.
- Per-project footprint block on Today → WS-E ("per-project footprint tracking").
- Renewals lists → derived from the subscriptions register. Traceable.
- `/ops` hub, register/status/report `active=` retargets, tab IA reshuffle → reorganization of already-specified views. Defensible under WS-E.

**Untraceable (FLAGS):**
1. **Wholesale visual redesign.** No acceptance criterion or scope line calls for restyling Slice 1 (which SPEC marks "BUILT and merged"). The diff rewrites the entire token set and typography — `globals.css`: `/* Strandworks cockpit — instrument-panel design system … Warm charcoal ground, brass accent */`, palette swapped (`--accent: #58a6ff` → `--brass: #d9a44a`, `--bg: #0d1117` → `#141210`), and `layout.tsx` adds three Google web fonts `import { Barlow, Barlow_Semi_Condensed, IBM_Plex_Mono } from "next/font/google"`. Nothing in Slices 1–2, WS-A–E, or acceptance criteria mandates an aesthetic overhaul. Not forbidden either — hence flag, not violation. (Note: `next/font/google` self-hosts; no "new paid service," so Slice-1 clause intact.)

2. **The attention queue / "Needs you" triage board — possibly PARKED.** `attention.ts`: `// The attention queue — the cockpit's triage board.`; `page.tsx`: `<h2>Needs you</h2>`. This is a new synthesized cockpit function aggregating decisions + calendar actions + money flags + single-copy assets into one ranked board. It is not enumerated in Slice 1, and it is not WS-C (which renders *pre-sprint alerts* specifically, not this cross-register triage). SPEC PARKED reads: *"Any additional cockpit function not listed above (documents viewer, trend charts, inbox dictation, workflow boards, …) — owner adds via Intake when wanted."* A cross-register triage board plausibly falls under "workflow boards" and should have entered via Intake. Its data is register-only (no invention), which mitigates, but the *function itself* is untraceable to a granted scope line.

## (b) NON-GOAL VIOLATIONS — 5/5
None found.
- No second user / public face — auth untouched, still owner-only.
- Not a credentials viewer — `money.ts` never carries `payment_last4`; the Money card renders service/plan/cadence/renewal/notes only, and notes pass through `redactForDisplay(it.notes)`. Consistent with the security floor.
- No DB of record — all reads via `readRepoFile` from git registers.
- No token-pressing / self-approval — new pages are read-only renders.
- Review gate untouched.

## (c) CANON TAMPERING — 5/5
Clean. The diff touches only `cockpit/**` (components, pages, `src/lib`, `tests`). VISION.md, SPEC.md, PORTFOLIO.md, CLAUDE.md, and enforcement config are NOT in the diff. No SPEC edit, so the missing intake-log row is not owed.

## (d) RESIDUE CREATION — 5/5
Clean. Every new file is code (`money/page.tsx`, `ops/page.tsx`, `attention.ts`, `money.ts`, two `.test.ts`). No `.md` created anywhere. Comments are descriptive-of-code, not directive — no "we will", "the plan is", "flagship", roadmaps, or task lists. Nothing persistent outside `_governance/`.

---

**Summary:** Canon-safe and residue-free. Downgraded from ALIGNED to ALIGNED-WITH-FLAGS on two untraceable scope items — an unrequested design-system rewrite of already-shipped Slice 1, and a new cross-register "triage board" that reads like a PARKED "workflow board" owed an Intake. Findings only; no fixes offered.

---

## Disagreement table

| Item | Reviewer A | Reviewer B | Disposition |
|---|---|---|---|
| Verdict | ALIGNED-WITH-FLAGS | ALIGNED-WITH-FLAGS | AGREE |
| (a) scope fidelity | 4/5 | 3/5 | Agree on substance; B scores lower |
| Visual/font re-skin untraceable to SPEC | FLAG (sideways-but-harmless) | FLAG (untraceable, not forbidden) | AGREE — both flag |
| Nav IA restructure (Today/Money/Ops tabs) | FLAG (sideways-but-harmless) | Defensible under WS-E (no flag) | ASYMMETRY — only A flags |
| Attention queue / "Needs you" board | Traceable to WS-E + VISION power 3 | FLAG — possibly PARKED "workflow board" owed an Intake | ASYMMETRY — only B flags |
| WS-E landing ahead of WS-A/WS-B safety spine | FLAG (sequencing observation) | not raised | ASYMMETRY — only A flags |
| (b) non-goals / (c) canon / (d) residue | 5/5 clean | 5/5 clean | AGREE |

Orchestrator note (no verdict softening): the work was a direct live owner
instruction this session ("I need a working dashboard … design is poor,
information incomplete, layout not intuitive"), which is the owner-ask context
for the re-skin, IA change, and triage-board flags. Owner rules at merge.
